export enum COLLECTION_NAME_KEYS {
  PRODUCTS = 'products',
  PRODUCT_VARIANTS = 'product_variants',
  USERS = 'users',
  CATEGORIES = 'categories',
  IMAGES = 'images',
}
export enum GENDER_KEYS {
  MEN = 'men',
  WOMEN = 'women',
}
export enum METADATA_KEYS {
  BASE_ROUTE = 'baseRoute',
  ROUTE_HANDLERS = 'routeHandlers',
}
export enum OPTION_TYPE_KEYS {
  SELECT = 'select',
  SELECT_WITH_IMAGE = 'select_with_image',
}
export enum STATUS_PRODUCT_KEYS {
  RAW = 'raw',
  EDITED = 'edited',
  PRODUCT = 'product',
}
